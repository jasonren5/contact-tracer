import FirebaseContext, { withFirebase } from './context';
import Firebase from './firebase';
import firebaseConfig from './config';

export default Firebase;

export { FirebaseContext, withFirebase, firebaseConfig };